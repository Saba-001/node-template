const PROJECT_PREFIX = "things-to-know:";

export const REDIS_KEY_BLACKLIST = PROJECT_PREFIX + "blacklist:";

export const REDIS_KEY_SESSION = PROJECT_PREFIX + "blacklist:";

export const REDIS_KEY_RATE_LIMIT = PROJECT_PREFIX + "blacklist:";

export const USER_PREFIX = PROJECT_PREFIX + "user:";

export const USERNAME_TO_ID_PREFIX = PROJECT_PREFIX + "user:username_to_id:";

export const USER_ID_GENERATOR_KEY = PROJECT_PREFIX + "user:count";

export const USER_REFRESH_TOKEN_FIELD = "refreshToken";
